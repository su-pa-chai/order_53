/*
function onOpen () { 
  SpreadsheetApp.getUi () 
  .createMenu ('เมนูใหม่ของฉัน') 
  .addItem ('แถบด้านข้างของฉัน 1','showSidebar') 
  .addToUi ();
} 

function showSidebar () { 
  var html=HtmlService.createHtmlOutputFromFile ('แถบด้านข้าง') 
  .setTitle ('แถบด้านข้างการคำนวณ'); 
  SpreadsheetApp.getUi () 
.showSidebar (html);
}  
*/
